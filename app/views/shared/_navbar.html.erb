<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to task_lists_path do %>
    <span class="navbar-brand">
      <span class="heavy">Tasks</span>App
    </span>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
        <li class="nav-item name">
          <%= link_to current_user.email, nil, class: "nav-link #{yield(:tasks_active)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Tasks", task_lists_path, class: "nav-link #{yield(:tasks_active)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "New Task List", new_task_list_path, class: "nav-link #{yield(:new_tasks_active)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Log In", user_session_path, class: "nav-link #{yield(:login_active)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign Up", new_user_registration_path, class: "nav-link #{yield(:signup_active)}" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>